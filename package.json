{
  "name": "paperboy",
  "private": true,
  "type": "module",
  "workspaces": ["packages/*", "apps/*", "tools/*"],
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^2.1.0",
    "vite": "^5.4.0",
    "@types/web-bluetooth": "^0.0.20"
  }
}
